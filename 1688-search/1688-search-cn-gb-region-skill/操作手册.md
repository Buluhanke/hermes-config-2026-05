# 1688 找品操作手册（照做即可，不需要理解原理）

> 本文件是「下次忘了怎么干」时直接照抄的步骤。
> **核心思路（2026-08-21 复盘后定稿）**：用 **AppleScript 驱动默认登录 Chrome（1688.com）** 跑搜货（尺寸词+彩盒定制双词）→ 矩阵式尺寸验证 → 抠价格。Playwright/9333 是脆弱备选，非优先。
> **铁律**：只认 1688.com，绝不碰 alibaba.com（国际站，不是一回事，用户原话「你开始乱来了」）。

## 0. 前置状态检查（每次开干前先跑最小冒烟）
```bash
# AppleScript 通道是否通（默认 Chrome 带登录态）
osascript -e 'tell application "Google Chrome" to return count of windows'
# 若报「通过 Apple 事件中的 JavaScript 的功能已关闭」→ 菜单 查看→开发者→允许 Apple 事件中的 JavaScript（坑15）
# 冒烟探针：盒骏 677701816838 是可靠探针，能抠到 SKU 即通道真通
```
- 不要一上来就批量跑 Playwright/9333 —— **先确认默认 Chrome 的 AppleScript JS 开关开着、登录态在**（坑36：先冒烟再全量）。
- 9333/Playwright 仅当 AppleScript 不可用且你明确要时才用，且批量必降速（坑35）。

## 1. 搜货（尺寸词 + 彩盒定制 双词，江浙沪）
- **矩阵式卖家只在「彩盒定制」池**，只搜 `25*13*32cm牛皮纸袋` 抓不到他们（坑31）。必须两组合并：
  - 词① `25*13*32cm牛皮纸袋`（GBK：25*13*32cm=%32%35%2A%31%33%2A%33%32%63%6D，牛皮纸袋=%C5%A3%C6%A4%D6%BD%B4%FC）
  - 词② `彩盒定制`（GBK `%B2%CA%BA%D0%B6%A8%D6%C6`）
- 端点：`https://s.1688.com/selloffer/offer_search.htm?keywords=<GBK>&province=%E6%B1%9F%E8%8B%8F,%E6%B5%99%E6%B1%9F,%E4%B8%8A%E6%B5%B7`（province=江浙沪）
- 翻页：URL 加 `&beginPage=2` 等。
- 提取 offerId：AppleScript 注入 `scripts/extract_ids.js` 读 `document.documentElement.outerHTML`，正则 `detail.1688.com/offer/(\d+)`。
- **提纯**：搜结果 JSON 不能直接当 ID 喂详情页（坑32）。用 python 取 `ids` 字段过滤纯数字 9-14 位，写每行一个文件（如 `/tmp/ids.txt`）。

## 2. 尺寸验证（矩阵式 + 连写双识别）
- 用 `scripts/verify_carton_matrix.js`（已修全角左括号 `（` 漏判，坑30）。
- 驱动：AppleScript 读文件列表逐 URL 注入（仿 `check_batch.scpt`，`delay 8` 防验证码），输出 `offerId \t JSON`。
- JSON 字段：`isCarton`(尺寸命中) / `matrixHit`(矩阵式长宽×高) / `connectedHit`(连写L*W*H) / `title`。
- **命中 ID ≠ 该尺寸有货**！矩阵卖家 SKU 轴范围不同（盒骏高3-22cm常规，瀚拓高350cm+立柱）。须用价格脚本验证具体尺寸组合真存在。

## 3. 价格 / 库存提取
- **首选**：AppleScript 驱动默认 Chrome + `scripts/price_clean3.js`（DOM 抠价，已验证稳，不触发风控）。
- **备选（脆弱，坑33）**：`scripts/price_mtop_capture.py` 用 Playwright 接管 9333 实例抠内联 `skuMapOriginal`。仅当 AppleScript 不可用且你明确要时用，且批量必降速（每页 6-8s + Captcha 检测）。
  ```bash
  python3 scripts/price_mtop_capture.py <offer_id> [25*13*32]
  # 不传尺寸 -> 回显全部 SKU；传 25*13*32 -> 只回显该尺寸组合
  ```
- 实测：677701816838 的 8×8×9=¥0.19/库存78万、9×9×10=¥0.23/库存54万。

## 4. ⚠️ 绝对不要做的事（本会话踩过的方向性错误）
- **绝不打开 alibaba.com**（国际站）。所有操作在 1688.com。遇到要"过验证码"只指默认 Chrome 里的 1688.com 标签页。
- **绝不用 Playwright 快速批量开详情页**（每页<3s 必触发 Captcha 墙，整实例废掉，坑35）。
- **启动 9333 实例前先 `rm -f ~/.chrome_1688_scraper/Singleton*`**（残留锁会让 Chrome 看锁即退）。
- **不要连 127.0.0.1:9222**（默认 Chrome 僵尸端口，非标准 CDP，MCP 也连不上）。

## 5. 常见坑（踩过的，别再踩）
- 坑30：矩阵式尺寸 `9cm（高）` 是全角左括号 `（`（左括号），正则必须 `[（）()]*` 都放。
- 坑31：矩阵卖家（彩盒定制类）只在「彩盒定制」搜索池，尺寸词搜不到 → 双词合并。
- 坑32：搜结果 JSON 不能直接当 ID 喂详情页 → 先提纯成纯数字 ID 列表。
- 坑34：alibaba.com ≠ 1688.com，别搞混站点到错窗口。
- 坑35：批量详情页必须降速（每页 6-8s），否则整实例进 Captcha 墙。
- `networkidle` 在 1688 永远不空闲 → 用 `domcontentloaded` + 固定等待。

## 6. 文件地图
- `scripts/extract_ids.js` —— 搜结果页提取 offerId
- `scripts/verify_carton_matrix.js` —— 矩阵+连写双识别尺寸验证
- `scripts/check_batch.scpt` —— AppleScript 批量开详情页核对（delay 8 降速，仿它写新任务）
- `scripts/price_clean3.js` —— 首选价格提取（DOM 抠价，不触发风控）
- `scripts/price_mtop_capture.py` —— Playwright 抠内联 skuMapOriginal（脆弱备选，坑33）
- `references/upgrade_2026_research.md` —— 8 环节全网调研对比
- 备份：`/Users/aimac/.hermes/skills/1688-search-backup-20260821_210000/`
