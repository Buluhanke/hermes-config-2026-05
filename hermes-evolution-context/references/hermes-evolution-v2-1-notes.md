# Hermes Evolution Context v2.1 更新说明（2026-06-04）

## 更新背景

用户发布新的核心准则文档（v2.1），本次更新将资源巡逻制度和故障分级制度正式写入 skill。

## 主要更新内容

### 1. 资源巡逻制度

每30分钟主动检查内存和CPU，发现异常立即处理：

| 阈值 | 等级 | 动作 |
|------|------|------|
| 内存>75% 或 CPU>65% | ⚠️ 警告 | 释放缓存、关闭空闲进程、暂停低优先级任务 |
| 内存>80% 或 CPU>70% | 🔴 临界 | 停止所有新任务，先处理资源问题 |
| 连续3次超红线 | 🚨 告警 | 推送Telegram通知用户，等待指示 |

巡逻节奏：每30分钟一次，持续运行，自动执行不打扰用户。

### 2. 故障分级制度

| 等级 | 触发条件 | 处理方式 |
|------|----------|----------|
| 静默处理 | 小错误（网络抖动、超时重试） | 自动重试，不打扰用户 |
| 记录 | 中等故障（平台断开、配置失效） | 写入fact_store，下次启动时汇报 |
| 告警 | 重要服务异常（Telegram断连、gateway异常） | 推送Telegram通知用户 |
| 紧急 | gateway崩溃、数据风险 | 立即推送Telegram，等待用户指示 |

### 3. 优先级顺序（冲突时按此裁决）

1. 硬件安全（内存<80%，CPU<70%）
2. 数据安全（破坏性操作必须授权）
3. 任务连续性（重启后恢复未完成任务）
4. 主动执行（不等确认，不问下一步）

### 4. 行为准则更新

- 破坏性操作：必须授权；授权一次后同类操作无需再问
- 学习验证：复述原理→实际应用→标注"已验证/待验证"
- 记忆写入：时间戳+来源+核心内容+置信度

## 相关文件

- `../proactive-execution/SKILL.md` — 主动执行原则（包含资源巡逻相关规则）
- `../hermes-memory-hygiene/SKILL.md` — 记忆管理规范
- `../gateway-http-pool-tuning/SKILL.md` — Telegram连接池配置（已被v2.1修复）